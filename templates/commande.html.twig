{% extends 'base.html.twig' %}

{% block body %}
    <h2>Finaliser votre commande</h2>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Valider la commande</button>
    {{ form_end(form) }}
{% endblock %}
